name: Update CV

on:
  push:
    branches:
      - main
    paths:
      - 'cv.yaml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update_cv:
    name: Generate and update CV PDF
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install RenderCV
        run: pip install "rendercv[full]"

      - name: Generate PDF from CV
        run: rendercv render cv.yaml

      - name: Copy PDF to examples folder
        run: |
          mkdir -p examples
          if [ -f "rendercv_output/cv.pdf" ]; then
            cp rendercv_output/cv.pdf examples/cv.pdf
          elif [ -f "rendercv_output/$(basename cv.yaml .yaml).pdf" ]; then
            cp "rendercv_output/$(basename cv.yaml .yaml).pdf" examples/cv.pdf
          else
            # Find the generated PDF file
            PDF_FILE=$(find rendercv_output -name "*.pdf" -type f | head -n 1)
            if [ -n "$PDF_FILE" ]; then
              cp "$PDF_FILE" examples/cv.pdf
            else
              echo "Error: No PDF file found in rendercv_output"
              exit 1
            fi
          fi

      - name: Set Git credentials
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions [Bot]"

      - name: Commit and push changes
        run: |
          git add examples/cv.pdf
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update CV PDF"
            git push
          fi

